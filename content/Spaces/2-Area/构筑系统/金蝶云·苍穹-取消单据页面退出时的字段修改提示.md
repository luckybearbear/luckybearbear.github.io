---
publish: true
aliases: ""
created: 2025-12-13T15:12:28.637+08:00
modified: 2026-03-02T13:42:27.321+08:00
cssclasses: ""
---


# 一、需求

我们在二开的时候通常会通过值更新联动一些字段值改动，比如说我手动输入A字段的值，会改动B字段的值，而B字段出于业务的需求是隐藏，这样会有一个问题，就是单据操作者在退出页面的时候，会提示A,B字段都改动，为了让操作者有更好的体验，就需要我们在退出页面的时候，只提示可见的字段有变动，而不必提示操作者看不到的字段。

# 二、思路与方案

我们在单据界面不管是点退出按钮还是点右上角的X，其实最终调用的都是kd.bos.form.operate.formop.Close操作方法

在这个方法里面会进入kd.bos.mvc.bill.BillView的checkDataChanged来检查有哪些数据做过改动。之后再做提示

我们可以发现几点，

1. 通过查看过来的不会校验数据改动，
2. 单据状态为审核态的不会校验数据改动，
3. 不光单据头，单据体，子单据体上字段改动都会提示，单据体，子单据体上删除行也会提示
4. 如果把数据包中字段属性的DataEntityState中的BizChang  改成false就可以跳过这个校验

# 三、实现过程

```java
@Override  
public void beforeClosed(BeforeClosedEvent e) {  
    super.beforeClosed(e);  
    DynamicObject dataEntity = this.getModel().getDataEntity(true);  
    DynamicObjectCollection entryEntities = dataEntity.getDynamicObjectCollection(PxtcMaintDailyConstant.ENTRYENTITY_DT);  
    for (DynamicObject entryEntity : entryEntities) {  
        DynamicObjectCollection subEntryEntities = entryEntity.getDynamicObjectCollection(PxtcMaintDailyConstant.PXTC_SUBENTRYENTITY_DT);  
        for (DynamicObject rowEntity : subEntryEntities) {  
            // "可编辑"字段退出不提醒  
            DynamicProperty enableProp = rowEntity.getDynamicObjectType().getProperty("pxtc_enable");  
            rowEntity.getDataEntityState().setBizChanged(enableProp.getOrdinal(), false);  
        }  
    }  
}
```
